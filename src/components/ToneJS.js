import Tone from 'tone';

let ToneJS = new Tone.Synth();
      //creating piano sound with tone.player
      // synth: new Tone.PolySynth(1, Tone.Sampler, {'C4': 'URL.to.C4.sample.mp3'})

export default ToneJS;